/**
 * 配置模板：复制为 config.js 后填写
 * 1. 在 GitHub 新建一个 Gist，文件名 inventory.json，内容先写 {}
 * 2. 复制 Gist ID（URL 最后一段）
 * 3. 创建 Personal Access Token，只需勾选 gist 权限
 */
window.INVENTORY_CONFIG = {
  gistId: 'YOUR_GIST_ID',
  githubToken: 'YOUR_GITHUB_TOKEN',
};
